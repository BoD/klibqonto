enableFeaturePreview('GRADLE_METADATA')

include ':library'
project(':library').name = 'klibqonto'

include ':sample-jvm'
[':sample-jvm'].forEach {
    project(it).projectDir = file("samples${it.replace(':', "/")}")
}
